version: "3.9"

services:
  chw-app:
    build: .
    container_name: chw_system
    ports:
      - "5000:5000"
    volumes:
      - ./chw.db:/app/chw.db
    environment:
      - FLASK_ENV=production
    restart: unless-stopped